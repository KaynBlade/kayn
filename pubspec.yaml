name: secondhand_market
description: "A new Flutter project for SWE311 Assignment 2."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Local Persistence Database
  sqflite: ^2.4.2+1
  path: ^1.9.0
  
  # Local Media Image Picker
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true